# Repository Guidelines

The rules for this repository — what the site is, its layout and link conventions,
the local-serve/verify commands, the "edit rule" workflow, and the commit/publish
convention — live in a single home: **[`CLAUDE.md`](CLAUDE.md)**. Read that file.

Nothing repository-specific is duplicated here, so the two files cannot drift apart.
The previous long-form version of this file is archived in `AGENTS.history.md`.
